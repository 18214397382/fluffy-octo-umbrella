{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && mv dist dist_bak"
  },
  "deploy": {
    "startCommand": "mv dist_bak dist && node api/index.mjs",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}