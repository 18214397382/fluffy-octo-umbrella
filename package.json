{
  "name": "smart-video-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node api/index.mjs",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}